# CLAUDE.md - chulgil.me {#overview}

> 마지막 업데이트: 2026-01-03

개인 웹사이트/포트폴리오 (chulgil.me)

## 빠른 참조 {#quick-reference}

| 항목 | 현재 (v1) | 계획 (v2) |
|------|----------|----------|
| 프레임워크 | Nuxt.js 2.x | Next.js 14 |
| UI | Bulma CSS | Tailwind CSS |
| 3D | - | React Three Fiber |
| 배포 | Docker | Vercel |
| 도메인 | https://chulgil.me | 동일 |

## 명령어 {#commands}

### v1 (nuxt-blog/)
```bash
cd nuxt-blog
yarn install              # 의존성 설치
yarn dev                  # 개발 서버 (localhost:3000)
yarn build && yarn start  # 프로덕션 빌드
yarn generate             # 정적 생성
docker-compose up -d      # Docker 실행
```

### v2 (계획)
```bash
cd v2
npm install               # 의존성 설치
npm run dev               # 개발 서버
npm run build             # 프로덕션 빌드
```

---

## 프로젝트 구조 {#project-structure}

```
chulgil-me/
├── nuxt-blog/           # v1 현재 운영 중인 사이트
│   ├── assets/          # 정적 에셋 (이미지 등)
│   ├── components/      # Vue 컴포넌트
│   ├── layouts/         # 페이지 레이아웃
│   ├── pages/           # 페이지
│   ├── plugins/         # Vue 플러그인
│   ├── static/          # 정적 파일
│   └── store/           # Vuex 스토어
│
├── v2/                  # v2 새 버전 (개발 예정)
│   └── src/             # Next.js 14 App Router
│
└── docs/                # 프로젝트 문서
    └── specs/
        ├── WEBSITE_SPEC.md  # v2 상세 명세
        └── TASKS.md         # 작업 현황
```

---

## Issue 기반 작업 {#issue-workflow}

작업은 GitHub Issue 단위로 진행합니다.

### 라벨 체계

| 카테고리 | 라벨 |
|----------|------|
| 타입 | `bug`, `feature`, `enhancement`, `refactor` |
| 버전 | `v1`, `v2` |
| 우선순위 | `priority: high/medium/low` |
| 도메인 | `domain: 3d`, `domain: animation`, `domain: audio` |

### 브랜치 네이밍

```bash
feat/42-hero-section      # 새 기능
fix/15-mobile-responsive  # 버그 수정
refactor/28-component     # 리팩토링
```

---

## Claude 작업 지침 {#claude-guidelines}

### 📋 작업 시작 전 체크리스트

1. **문서 확인**:
   ```
   docs/specs/WEBSITE_SPEC.md  → v2 상세 명세 (필수!)
   docs/specs/TASKS.md         → 작업 현황
   README.md                   → 프로젝트 개요
   ```

2. **버전 확인**:
   - v1 (nuxt-blog/): 현재 운영 중, 유지보수만
   - v2 (v2/): 새 개발, 명세서 기반으로 진행

### 📁 새 코드 작성 위치 (v2)

| 항목 | 위치 |
|------|------|
| 페이지 | `v2/src/app/` |
| 섹션 컴포넌트 | `v2/src/components/sections/` |
| 3D 컴포넌트 | `v2/src/components/three/` |
| UI 컴포넌트 | `v2/src/components/ui/` |
| 애니메이션 | `v2/src/components/animations/` |
| 훅 | `v2/src/hooks/` |
| 유틸 | `v2/src/lib/` |

### ✅ 작업 완료 체크리스트

1. [ ] 린트 검사 통과 (`npm run lint`)
2. [ ] 반응형 확인 (모바일, 태블릿, 데스크톱)
3. [ ] 접근성 확인 (키보드 내비게이션, ARIA)
4. [ ] 성능 확인 (60fps 애니메이션)
5. [ ] 커밋 메시지 한글로 작성

---

## 핵심 규칙 {#core-rules}

### 응답 언어
- **Claude 응답: 항상 한글**
- 코드 주석: 영어 유지
- 커밋 메시지: 한글 (Conventional Commits)

### 디자인 (v2)
- 테마: "현의 조화 (String Harmony)"
- 색상: Rosewood, Amber, Ivory, Ebony
- 폰트: Playfair Display (제목), Source Serif Pro (본문)
- 상세: [docs/specs/WEBSITE_SPEC.md](docs/specs/WEBSITE_SPEC.md)

### 3D & 애니메이션
- React Three Fiber + Drei
- GSAP ScrollTrigger
- Lenis (부드러운 스크롤)
- Framer Motion (트랜지션)

### 오디오
- Howler.js
- 기본 음소거, 사용자 토글
- 앰비언트 + 인터랙션 사운드

---

## 문서 구조 {#docs-structure}

| 문서 | 내용 | 확인 시점 |
|------|------|----------|
| `docs/specs/WEBSITE_SPEC.md` | v2 전체 명세 | 🔴 필수 확인 |
| `docs/specs/TASKS.md` | 작업 현황/체크리스트 | 작업 전 |
| `README.md` | 프로젝트 개요 | 개요 파악 시 |

---

## v2 개발 단계 {#development-phases}

### Phase 1: 기반 구축
- [ ] Next.js 14 프로젝트 설정
- [ ] Tailwind CSS 설정
- [ ] 타이포그래피 & 색상 시스템
- [ ] Lenis 부드러운 스크롤

### Phase 2: 핵심 섹션
- [ ] 히어로 섹션 (주파수 바이올린)
- [ ] 소개 섹션
- [ ] 스킬 오케스트라
- [ ] 프로젝트 악장
- [ ] 연락처 피날레

### Phase 3: 3D 통합
- [ ] React Three Fiber 설정
- [ ] 주파수 바이올린 3D
- [ ] 파티클 시스템
- [ ] 스크롤 인터랙션

### Phase 4: 애니메이션 & 오디오
- [ ] GSAP ScrollTrigger
- [ ] Howler.js 오디오
- [ ] 마이크로 인터랙션

### Phase 5: 최적화 & 배포
- [ ] 성능 최적화 (Lighthouse 90+)
- [ ] 접근성 감사
- [ ] Vercel 배포

---

## 성능 목표 {#performance-goals}

| 지표 | 목표 |
|------|------|
| LCP | < 2.5초 |
| FID | < 100ms |
| CLS | < 0.1 |
| FPS | 60fps |
| 초기 번들 | < 200kb |

---

## 작업 우선순위 {#priorities}

| 순위 | 작업 | 상태 |
|:----:|------|:----:|
| 1 | v2 프로젝트 설정 | 대기 |
| 2 | 히어로 섹션 | 대기 |
| 3 | 주파수 바이올린 3D | 대기 |
| 4 | 나머지 섹션 | 대기 |

---

## 문제 해결 {#troubleshooting}

### v1 (Nuxt.js)
```bash
# 의존성 문제
rm -rf node_modules yarn.lock && yarn install

# Docker 재빌드
docker-compose down && docker-compose up -d --build
```

### v2 (Next.js)
```bash
# 캐시 클리어
rm -rf .next node_modules && npm install

# 3D 성능 문제
# - GPU 가속 확인
# - 모델 LOD 적용
# - 파티클 수 감소
```

---

## 핵심 컨셉: 주파수 (Frequency) {#core-concept}

> **"모든 것은 주파수로 연결된다"**

| 레이어 | 요소 | 주파수 표현 |
|--------|------|-------------|
| Human | 한글 텍스트 | 부드러운 사인파 |
| AI | 이진수, 코드 | 각진 사각파 |
| Music | 음표, 기호 | 순수 사인파 |

세 레이어가 **주파수**라는 공통 언어로 연결되어 바이올린 형태를 구성합니다.

→ 상세: [docs/specs/WEBSITE_SPEC.md](docs/specs/WEBSITE_SPEC.md)
